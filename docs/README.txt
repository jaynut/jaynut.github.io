SpiroToy Changelog

-supports save/load operation via string
-compatible with java application
-added input fields for everything
-rewrote the html/js base to allow for creating the wheels via JS, allowing for infinite wheels if needed (hardcapped at 20 currently)
-changed animate settings to allow/disallow a single wheels
-added a polarization/depolar option which projects the radial formula with X=theta, Y = Radius
